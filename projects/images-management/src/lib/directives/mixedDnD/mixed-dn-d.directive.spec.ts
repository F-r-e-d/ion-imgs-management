import { MixedDnDDirective } from './mixed-dn-d.directive';

describe('MixedDnDDirective', () => {
  it('should create an instance', () => {
    const directive = new MixedDnDDirective();
    expect(directive).toBeTruthy();
  });
});
